algoritmo "Tipos de Variáveis"

var x: real
    y: inteiro
    a: caractere
    l: logico

inicio

    x <- 2.5
    y <- 6
    a <- "teste"
    l <- 0

    escreval (x)
    escreval (y)
    escreval (a)
    escreval (l)

fimalgoritmo